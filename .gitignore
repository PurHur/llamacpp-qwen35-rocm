# Models and weights — never commit these
*.gguf
*.ggml
*.bin
*.safetensors
models/
**/models/**/*.gguf

# Upstream clone (fetched at Docker build time from patches/)
llama.cpp/

# Build artifacts
llama.cpp/build*/
llama.cpp/.cache/
build/
*.o
*.so

# Profiling scratch (keep summary JSON only)
var/profiles/*.csv
var/profiles/*_*/
var/profiles/*.json
*.pid
